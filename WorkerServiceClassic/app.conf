akka {
    loglevel = DEBUG
    stdout-loglevel = DEBUG
    actor {
        debug {
            receive = on
            autoreceive = on
            lifecycle = on
            event-stream = on
            unhandled = on
        }
    }
}